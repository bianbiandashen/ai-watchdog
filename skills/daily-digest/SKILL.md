name: daily-digest
version: 1.0.0
description: Daily knowledge digest — what you learned across all projects, value score, trend tracking
input: json (stdin) — {"days": 1}
output: json (stdout) — per-project digest, value scores, trend data
