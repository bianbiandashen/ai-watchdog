name: anomaly-detector
version: 1.0.0
description: Detect anomalous process behavior and resource usage
input: json (stdin) — health report JSON
output: json (stdout) — anomalies list, severity, recommendations
