name: session-analyzer
version: 1.0.0
description: Analyze recent AI coding sessions for patterns and activity
input: json (stdin) — {"hours": 6}
output: json (stdout) — session counts, active projects, message stats
