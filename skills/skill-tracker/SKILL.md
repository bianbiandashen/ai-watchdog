name: skill-tracker
version: 1.0.0
description: Track daily tool and skill usage across Claude/Codex sessions, rank top 5 with suggestions
input: json (stdin) — {"hours": 24}
output: json (stdout) — tool counts, skill counts, top 5 with suggestions
